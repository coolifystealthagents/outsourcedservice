import { blogPosts, generatedBlogPosts, batchBlogPosts } from '../../../data';import {BlogListing,PAGE_SIZE} from '../../blog-listing';export function generateStaticParams(){const totalPosts=blogPosts.length+generatedBlogPosts.length+batchBlogPosts.length;return Array.from({length:Math.max(1,Math.ceil(totalPosts/PAGE_SIZE))},(_,i)=>({page:String(i+1)}))}export default async function NumberedBlogPage({params}:{params:Promise<{page:string}>}){const {page}=await params;const n=Number(page);return <BlogListing page={n}/>}
