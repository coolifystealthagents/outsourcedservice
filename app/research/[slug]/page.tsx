import {notFound} from 'next/navigation';import {Header,Footer} from '../../components';import {researchPosts} from '../../fleet-content';export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export default async function ResearchArticle({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=researchPosts.find(p=>p.slug===slug);if(!post)notFound();return <><Header/><main className="fleet-main"><article className="section article-shell"><p className="eyebrow">Philippines staffing research · {post.published}</p><h1>{post.title}</h1><p className="lead">{post.excerpt}</p><p><strong>Headline metric:</strong> {post.headlineStat}</p>{post.body.map((x,i)=><p key={i}>{x}</p>)}<h2>Key stats and source notes</h2><p>{post.methodology}</p><ol>{post.sources.map((source,i)=><li key={source.url}><a href={source.url} rel="noreferrer">{i+1}. {source.label}</a></li>)}</ol><h2>FAQs</h2>{post.faq.map(item=><section key={item.question}><h3>{item.question}</h3><p>{item.answer}</p></section>)}<h2>Related Research</h2><div className="fleet-card-grid">{post.related.map(related=>{const item=researchPosts.find(candidate=>candidate.slug===related);return item?<a className="fleet-card" href={`/research/${item.slug}`} key={item.slug}><h3>{item.title}</h3><p>{item.excerpt}</p></a>:null})}</div></article></main><Footer/></>}
